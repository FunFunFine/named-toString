# `@withNamedToString`

A standalone macro that adds parameter names for case classes.

```scala
"Foo(3,abc)" >> "Foo(bar=3, baz=abc)"
```

## Installation 

`libraryDependencies += "io.github.funfunfine" % "named-tostring_2.13" % "0.1.0"` 
